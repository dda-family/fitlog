# CLAUDE.md — Fitlog 개발 지침

> 이 문서는 Fitlog 개발의 최상위 규칙이다.
> 기능 구현 전 항상 이 문서와 `docs/` 스펙, `docs/DECISIONS.md`를 먼저 확인한다.
> 최종 갱신: 사전 개발 검토 결정 6건 반영 (자세한 내역은 `docs/DECISIONS.md`).

## 1. 프로젝트 한 줄 정의
Fitlog는 한 명의 사용자가 iPhone에서 운동 중 빠르게 세트·중량·반복수·유산소를 기록하고, 현재 운동 가이드와 실제 수행을 비교하며, 세트 간 휴식 타이머를 사용하는 **오프라인 우선 개인용 PWA**다.

## 2. 최우선 원칙
이 프로젝트에서 가장 중요한 것은 기능의 많음이 아니라 **운동을 방해하지 않는 것**이다.

개발 시 아래 원칙을 항상 우선한다.

1. 한 손으로 조작할 수 있어야 한다.
2. 운동 중 핵심 동작은 가능한 한 1~2번의 터치로 끝나야 한다.
3. 로그인, 서버, 소셜 기능, 계정 기능은 만들지 않는다.
4. 사용자 운동 데이터는 iPhone 로컬에 저장한다.
5. 인터넷이 없어도 운동 기록·조회·타이머가 동작해야 한다.
6. 디자인을 과도하게 꾸미지 않는다. 깔끔하고 직관적인 UI가 우선이다.
7. 불필요한 모달, 애니메이션, 페이지 전환을 만들지 않는다.
8. 사용자가 요청하지 않은 기능을 임의로 추가하지 않는다.
9. 가이드 변경은 자동 적용하지 않는다. 항상 사용자가 확인 후 적용한다.
10. 기존 저장 데이터의 호환성을 깨는 변경은 금지한다. 데이터 스키마 변경 시 마이그레이션을 고려한다.

## 3. MVP 기술 스택

### 필수
- Vanilla HTML
- Vanilla CSS
- Vanilla JavaScript
- PWA
- Service Worker
- IndexedDB
- Web App Manifest

### 사용하지 않음
- Next.js / React / Vue / Svelte
- Supabase / Firebase / PostgreSQL / 별도 백엔드
- 회원가입/로그인
- 외부 API / OpenAI API / Anthropic API
- 복잡한 상태관리 라이브러리 / UI 프레임워크

작은 라이브러리조차 꼭 필요하지 않으면 사용하지 않는다.
NoSleep 계열의 화면 유지 기능도 라이브러리를 쓰지 않고 인라인으로 직접 구현한다(§5 참고).

## 4. 실행 환경
- 개발: 사용자의 MacBook, **VSCode** (Claude Code 자동 편집은 사용하지 않음. 코드는 수동 반영)
- 실제 사용: 사용자의 iPhone 15
- 주 사용 브라우저/설치 환경: iOS Safari → 홈 화면에 추가한 PWA
- 실제 운동 중 네트워크 연결은 필수가 아니다.
- 앱 파일 업데이트를 위해 최초 설치/업데이트 시 HTTPS 정적 호스팅을 사용한다(GitHub Pages / Netlify / Vercel 등).
- 정적 호스팅에는 앱 파일만 올라가며 운동 기록 데이터는 업로드하지 않는다.
- 배포 URL만 있으면 다른 사람도 각자 설치해 독립적으로 사용 가능하다(데이터 공유 아님, 단순 앱 배포).

## 5. 오디오/타이머 핵심 요구사항 ★ (사전 검토로 갱신됨)

### 5.1 iOS 현실 (반드시 인지)
iOS의 웹앱(WKWebView) 실행 환경은 **화면이 잠기거나 앱이 백그라운드로 가면 JS 타이머와 오디오를 중단**시킨다. 화면 꺼짐을 막는 표준 Wake Lock API는 iOS Safari에서 지원되지 않는다. 따라서 "폰을 주머니에 넣어도 0초에 소리가 난다"는 보장은 불가능하다.

### 5.2 채택한 방식
- 휴식 종료 알림음은 **앱이 화면 켜진 포그라운드 상태일 때 확실히 재생**되는 것을 기준으로 한다.
- 타이머가 도는 동안 **화면을 강제로 깨워둔다.** 구현: 아주 짧은 무음 루프 `<video>`(muted, playsinline)를 사용자의 탭 시점에 `play()` 하여 화면 잠김을 막는다(NoSleep 방식, 라이브러리 없이 인라인 구현). 타이머 종료/취소 시 정지한다. 90초·180초라는 짧은 구간이라 이 방식으로 알림음이 안정적으로 울린다.
- 화면 상단에 남은 시간을 크게 고정 표시하여, 소리를 못 들어도 눈으로 0초를 확인할 수 있게 한다(시각적 안전장치).
- 타이머 자체는 `setInterval` 카운트가 아니라 `endsAt = startedAt + duration` 기준으로 계산하고, 앱 복귀(`visibilitychange`) 시 `endsAt - Date.now()`로 남은 시간을 재계산한다. 사용자가 잠깐 화면을 잠갔다 돌아와도 표시는 정확히 복구된다.

### 5.3 알림음 소스
- **iPhone 기본 시스템 알림음(트라이톤 등)은 웹앱에서 접근·재생할 수 없다.** 그 사운드는 네이티브 앱과 iOS 알림에만 열려 있다.
- 따라서 앱에 **자체 제작한 짧은 차임(`assets/sounds/rest-end.mp3`)을 asset으로 포함**하여 사용한다. Service Worker가 캐시하므로 오프라인에서도 재생된다.
- 진동은 사용하지 않는다.
- 소리가 마음에 들지 않으면 파일만 교체하면 된다.

### 5.4 오디오 언락
- iOS는 사용자 제스처 없이는 오디오를 재생할 수 없다. 최초 사용자 탭(예: `🎧 알림 테스트` 버튼, 또는 운동 시작 버튼) 시점에 알림음 오디오와 화면 유지 video를 한 번 프라임(재생/즉시정지)하여 이후 프로그래밍적 재생이 가능하도록 언락한다.
- `🎧 알림 테스트` 버튼은 필수로 제공한다.

### 5.5 기본 타이머 값
- 세트 사이 휴식: 90초
- 운동 사이 준비/휴식: 180초
- 두 값 모두 설정에서 변경 가능해야 한다.
- 타이머는 자동 추적이 아니라 사용자가 직접 버튼을 눌러 시작한다.

## 6. 제품 문서 우선순위
기능 구현 전 아래 문서를 반드시 확인한다.

1. `docs/DECISIONS.md` (가장 최근 확정 결정)
2. `docs/PRODUCT_SPEC.md`
3. `docs/UX_SPEC.md`
4. `docs/DATA_SPEC.md`

문서 간 충돌 시 우선순위:
1. 가장 최근 사용자 요구사항
2. DECISIONS.md
3. PRODUCT_SPEC
4. UX_SPEC
5. DATA_SPEC
6. 현재 구현

요구사항이 애매하면 임의로 복잡한 기능을 만들지 말고 가장 단순한 구현을 택한다.

## 7. 확정된 핵심 설계 결정 (요약)
아래 6건은 사전 개발 검토에서 확정되었다. 상세 근거와 영향 문서는 `docs/DECISIONS.md`.

1. **알림음**: 포그라운드 + 화면 유지(video) 기반 재생. 백그라운드 알림은 보장하지 않음. 큰 카운트다운을 시각 안전장치로 둠.
2. **루틴 = 템플릿 + 요일 매핑**: 루틴은 요일에 하드코딩하지 않고 독립 "루틴 템플릿" 엔티티로 둔다. `요일 → 템플릿` 매핑은 설정에서 변경 가능. **세션(기록)은 항상 실제 캘린더 날짜 + 그날 요일 + 실제 수행 템플릿**을 저장한다. → 화요일 루틴을 수요일에 해도 기록은 그날(수) 기준으로 남는다.
3. **어시스트 종목 판정 역방향**: `sideMode: assist` 종목(어시스트 딥스/풀업)은 증량 후보가 "보조중량 ↓ = 난이도 ↑" 방향으로 표시된다. 판정 임계값은 동일하되 권장 방향만 반대.
4. **미정 중량**: 시드에서 `null`. 첫 기록값을 "지난 사용값"으로 자동 채움. 원터치 "가이드 기본값으로 저장"으로 사용자가 직접 승격(자동 반영 아님).
5. **워밍업**: 전역 기능이 아니라 **종목별 `warmupEnabled` 플래그**. 벤치·스쿼트는 시드에서 ON. 워밍업 세트(`setType: warmup`)는 완료율·증량후보 판정에서 제외.
6. **알림음 소스**: 시스템 사운드 불가 → 자체 제작 차임 파일 사용.

## 8. 프로젝트 폴더 구조 (★ 루트 평탄화 — src/ 없음)

> 과거 src/(개발본)+루트(배포본) 이중구조는 v22에서 폐지. 앱이 로드하는 파일이 곧
> 원본이며 전부 저장소 루트에 있다. 배포·버전 규칙은 `docs/DEPLOY.md` 참고.

```text
fitlog/                     # 저장소 루트 = 앱 진입점 = GitHub Pages 서빙 위치
├── index.html              # 앱 진입점 (사이트 루트 URL에서 바로 열림)
├── styles.css
├── app.js                  # 앱 부트스트랩, 라우팅(탭), 오늘/기록 화면
├── db.js                   # IndexedDB 래퍼, 스토어 정의, 시드
├── timer.js                # endsAt 기반 타이머 + 화면 유지 + 알림음
├── evaluation.js           # 세트/운동 판정, 완료율
├── backup.js               # JSON export/import
├── ai-bridge.js            # AI 프롬프트 생성 + 제안 JSON 검증/적용
├── catalog-data.js heatmap.js
├── runtime/                # 3D 뷰어(Three.js). index.html 옆(루트)에 있어야 함
│   ├── heatmap-3d.js  muscle-bindings.js  svg-fallback.js
│   └── vendor/three/...
├── assets/                 # icons/, sounds/rest-end.mp3, video/, anatomy/(SVG 5종+GLB)
├── data/                   # muscle-regions.json, anatomy-3d-map.json
├── docs/                   # DECISIONS·PRODUCT_SPEC·UX_SPEC·DATA_SPEC·DEPLOY 등
├── CLAUDE.md  README.md
├── manifest.webmanifest    # start_url: "./index.html"
├── service-worker.js       # CACHE_VERSION (app.js의 APP_VERSION과 같은 숫자로 유지)
└── .gitignore
```

**개발/배포 워크플로우 (v22~):** 맥북 로컬에서만 작업한다. VSCode로 파일 수정 →
`git add -A && git commit -m "..." && git push` → GitHub Pages 자동 배포. zip·수동
업로드는 쓰지 않는다. 코드/자산 변경 시 `service-worker.js`의 `CACHE_VERSION`과
`app.js`의 `APP_VERSION`을 **같은 숫자로 +1** 한다(안 올리면 기존 PWA가 옛 캐시 유지).

## 9. 구현 순서
한 번에 모든 기능을 만들지 않는다.

### Phase 1 — 운동 중 코어 (오프라인, 저장 없음도 허용)
- 정적 모바일 UI, 하단 네비게이션
- 오늘 화면: 요일 매핑에 따른 템플릿 제안 + 수동 선택
- 운동 카드: 가이드/지난번/오늘 입력(중량·반복 +/- 및 키패드)
- 세트 완료 → 90초 휴식 타이머 (화면 유지 + 알림음)
- 마지막 세트 → 180초 운동 간 타이머
- 워밍업 세트 입력(벤치·스쿼트)
- `🎧 알림 테스트`

### Phase 2 — 영구 저장/판정
- IndexedDB 스토어 + 시드(§DATA_SPEC)
- 날짜별 세션 저장(날짜·요일·템플릿)
- 지난번 기록 표시, 미정 중량 자동 채움
- 가이드 대비 판정(어시스트 역방향·워밍업 제외 포함)

### Phase 3 — 기록/편집/백업
- 기록 화면(날짜별, 상태 표시)
- 가이드 편집: 종목·중량·세트·반복·필수/선택·순서·워밍업·메모
- **템플릿 추가/삭제/이름변경 + 요일 매핑 편집**
- 미정 중량 "가이드 기본값으로 저장" 승격
- JSON Export/Import 백업

### Phase 4 — AI 코치
- 분석용 프롬프트 생성(Export)
- 제안 JSON 붙여넣기 → 검증 → diff 확인 후 적용
- 가이드 변경 이력 저장

## 10. 범위 밖 (MVP에서 구현하지 않음)
회원가입 / 여러 사용자 계정 / 친구·공유·SNS(데이터 공유) / 식단 / 칼로리 / 체중 자동 연동 / Apple Health / 카메라 / AI API 직접 호출 / 클라우드 동기화 / 푸시 서버 / 운동 자동 인식 / GPS / 복잡한 그래프 / 워치 앱 / Siri / 운동 시간 자동 측정 / 자동 세트 감지 / 백그라운드 알림 보장.

## 11. 완료 기준
- iPhone 세로 화면 폭에서 깨지지 않는다.
- 네트워크가 끊긴 상태에서도 기존 설치된 앱이 열린다.
- 운동 기록이 앱 종료 후에도 남는다.
- 세트 완료 버튼을 누르면 사용자가 설정한 휴식 타이머가 시작되고, 그동안 화면이 꺼지지 않는다.
- 타이머 종료 시(포그라운드) 자체 차임이 재생된다. 진동은 발생하지 않는다.
- 기록 삭제/백업 복원 등 데이터 손실 가능 작업에는 확인 절차가 있다.
- 기본 루틴이 PRODUCT_SPEC의 시드와 일치한다.
- 화요일 템플릿을 다른 요일에 수행해도 기록에 실제 날짜·요일이 남는다.
