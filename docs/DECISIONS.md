# DECISIONS.md — Fitlog 결정 로그

이 문서는 확정된 설계 결정을 시간순으로 남기는 로그다. 문서 간 충돌 시 여기가 PRODUCT/UX/DATA_SPEC보다 우선한다(가장 최근 사용자 요구사항 다음). 새 결정은 맨 아래에 추가한다.

---

## 2026-08-28 — 사전 개발 검토 (D1~D6)

### D1. 알림음/타이머 방식 — 포그라운드 + 화면 유지 기반
**결정**: 휴식 종료 알림음은 앱이 화면 켜진 포그라운드일 때 재생을 보장한다. 타이머가 도는 동안 무음 루프 `<video>`(muted, playsinline)로 화면을 깨워둔다(NoSleep 방식, 라이브러리 미사용). 상단에 큰 카운트다운을 고정 표시해 소리를 못 들어도 눈으로 확인 가능하게 한다.
**근거**: iOS WKWebView는 화면 잠금/백그라운드 시 JS·오디오를 중단한다. Wake Lock API는 iOS Safari 미지원. 따라서 "주머니에 넣어도 0초에 소리"는 보장 불가. 90/180초 짧은 구간이라 화면 유지 방식이 현실적.
**포기한 대안**: Web Push(서버 필요, 로컬 예약 알림 아님) → 범위 밖.
**영향 문서**: CLAUDE §5, PRODUCT §7, UX §7/§9, DATA §10/§11.

### D2. 루틴 = 템플릿 + 요일 매핑, 세션은 실제 날짜 기록
**결정**: 루틴을 요일에 하드코딩하지 않고 독립 "루틴 템플릿" 엔티티로 둔다. `요일 → 템플릿` 매핑은 설정에서 변경 가능(기본 화·목·토). **세션(기록)은 항상 실제 캘린더 날짜 + 파생 요일 + 실제 수행 템플릿을 저장**한다.
**근거**: 화요일 루틴을 수요일에 할 수 있어야 하고, 그때 기록은 그날(수) 기준으로 남아야 함. 요일 변경/분할 확장(4분할 등)도 데이터 구조를 깨지 않고 지원. 배포 전이라 마이그레이션 없이 v1에 반영.
**영향 문서**: DATA §4.1/§4.3/§4.4/§4.5, PRODUCT §3/§12, UX §4/§15/§16.

### D3. 어시스트 종목 판정 역방향
**결정**: `sideMode: assist` 종목(어시스트 딥스/풀업)은 판정 임계값은 동일하되, 증량 후보의 권장 방향을 반대로(보조중량 ↓ = 난이도 ↑) 표기한다. 판정 결과에 `progressionDirection`(`increase`/`decrease_assist`) 포함.
**근거**: 어시스트 종목은 보조중량을 올리면 오히려 쉬워지므로, 일반 증량 문구를 그대로 쓰면 오해를 준다.
**영향 문서**: PRODUCT §8.3, DATA §8.3/§8.4.

### D4. 미정 중량 — 지난값 자동채움 + 수동 승격
**결정**: 미정 종목(이두 컬·삼두 푸시다운, 스쿼트 시작값)은 시드에서 중량 `null`. 첫 기록값을 지난 사용값으로 자동 채움. 카드/가이드에 "이 값을 가이드 기본값으로 저장" 원터치 버튼 제공. 자동 승격은 하지 않는다(CLAUDE §2-9 원칙 준수).
**근거**: 사용자의 "첫 사용값을 디폴트로" 요구를 충족하면서 가이드 자동변경 금지 원칙과 충돌하지 않게 함.
**영향 문서**: PRODUCT §5/§6.1/§8.4, DATA §6, UX §5.1.

### D5. 워밍업 — 종목별 플래그
**결정**: 워밍업은 전역 기능이 아니라 종목별 `warmupEnabled` 플래그. 벤치·스쿼트만 시드 ON(벤치는 빈 봉 20kg×10~12, 30kg×5~8 안내 포함). 워밍업 세트는 `setType: warmup`로 저장하고 완료율·증량후보 판정에서 제외. 가이드 편집에서 아무 종목에나 켜고 끌 수 있음.
**근거**: 사용자가 벤치·스쿼트만 워밍업 예외를 원함. 전역 기능은 UI 복잡도만 키움.
**영향 문서**: PRODUCT §5/§8, DATA §4.2/§4.4/§7, UX §5.2.

### D6. 알림음 소스 — 자체 제작 차임
**결정**: iPhone 기본 시스템 알림음은 웹앱에서 접근 불가하므로, 자체 제작한 짧은 부드러운 차임(`assets/sounds/rest-end.mp3`)을 앱 asset으로 포함해 사용한다. SW 캐시로 오프라인 재생. 소리는 파일 교체로 언제든 변경 가능.
**근거**: iOS 시스템 사운드 라이브러리는 네이티브 앱/iOS 알림 전용. 애플 벨소리 직접 사용은 저작권 문제도 있음.
**영향 문서**: CLAUDE §5.3, PRODUCT §7.3, DATA §11.

---

## 2026-08-28 (2차) — D7~D9 확정

### D7. 타이머 = start-and-forget (제어 버튼 없음)
**결정**: 타이머에 일시정지·정지(끄기) 버튼을 두지 않는다. 시작하면 0초까지 자동으로 흐르고, 0초에 차임이 **단 한 번만** 울린다(반복/재알림 없음). 실행 중 타이머는 다음 `세트 완료`/`운동 완료`가 새 타이머를 시작하면 자연 대체된다.
**근거**: 사용자 실제 흐름이 "기록 → 시작 탭 → 폰 내려놓고 휴식 → 한 번 울리면 다시 운동"의 반복이라, 멈추거나 끌 대상이 없다. 버튼이 많을수록 운동을 방해한다.
**영향 문서**: UX §9, `timer.js`(addSeconds/정지 UI 제거).

### D8. 시드 데이터 확정
**결정**: `db.js`의 `SEED`를 PRODUCT_SPEC §5 표대로 채웠다. 미정 중량은 이두 컬·삼두 푸시다운 = `null`, 스쿼트 = `40`(시작값). 워밍업은 벤치·스쿼트만 `warmupEnabled: true`이며 `warmupSuggestions`로 표시(벤치: 빈 봉 20kg×10~12, 30kg×5~8 / 스쿼트: 빈 봉 20kg×10, 30kg×5 — 스쿼트 워밍업 수치는 기본 제안값, 편집 가능). 시드는 최초 1회만 깔리며(seedIfEmpty) 이후 가이드 화면에서 자유 편집·타인 수정 가능.
**영향 문서**: DATA §20, `db.js` SEED.

### D9. 기록 화면 MVP = 날짜별 조회만
**결정**: 기록 화면은 MVP에서 날짜별 조회만 제공한다. 주간 총 세트·부위별 집계·증량 후보 목록 등 통계는 이후로 미룬다.
**근거**: 초보 초기 단계에선 통계보다 기록 자체가 우선.
**영향 문서**: PRODUCT §11(통계는 "추가 가능"으로 유지, MVP 미포함).

---

## 확정: 백업 복원 = 전체 덮어쓰기
Merge(기존 데이터에 백업을 섞기)는 충돌 규칙이 복잡해 1인용 앱에 과함. 복원은 전체 덮어쓰기로 유지(DATA §14).

## 미해결/추후 결정 (열어둠)

---

## 2026-08-28 (3차) — 오늘 화면 개선 D10~D14 (Phase 1 실사용 피드백)

### D10. 날짜 직접 선택
오늘 화면 상단 날짜를 탭하면 날짜 선택기(네이티브 date)로 변경 가능. 기본값은 오늘. **템플릿 추천은 첫 디폴트로 고정**(날짜를 바꿔도 템플릿은 안 따라감 — "원래 그날 하려던 운동"의 의미 유지). 세션은 선택한 날짜·파생 요일로 기록.

### D11. 유산소 상시 카드 + 완료 순서
유산소를 종료 화면이 아니라 오늘 화면 맨 아래에 **상시 카드**로 노출(경사/속도/시간 입력 + `✓ 유산소 완료`). 유산소를 먼저 하는 날도 있으므로 완료 순서 자유. 흐름: 운동별 완료 → 유산소 완료 → 최종 "오늘 운동 종료". 종료 요약엔 유산소 완료 여부만 표시.

### D12. 즉석 운동 추가 (＋)
헤더 우측 `＋ 운동 추가`로 이번 세션에만 존재하는 운동 카드 추가. 이름 자유 입력(글자수 제한 없음, 예: "무릎모아 앉았다 일어나기"), kg·반복·세트(기본 3) 그 자리 입력. **가이드 정식 등록 아님**(Phase 3). 선택 운동으로 취급해 필수 완료율에 영향 없음. 삭제 가능.

### D13. 워밍업 중량·반복 입력 + 시각 구분
워밍업도 본세트처럼 중량·반복 스텝퍼로 실제 값 입력. 첫 추천값 자동 프리필. 본세트와 **확실히 구분되는 UI**(점선/앰버 톤/"W" 태그). 판정에서는 계속 제외(`setType: warmup`).

### D14. 목표 세트 초과 (＋ 세트 추가)
목표 세트를 다 채운 뒤 `＋ 세트 추가`로 4·5세트째 기록 가능. **추가 세트는 기록에 모두 저장**되지만, **증량후보/완료율 판정은 목표 세트(첫 N개)만** 기준으로 계산(추가 세트가 판정을 흔들지 않음). 화면엔 "(추가)"로 구분. 추가 세트 휴식은 90초 통일.

**영향 문서**: UX §4/§5/§9/§13, `app.js`, `styles.css`.

---

## 2026-08-28 (4차) — Phase 2 완료 + 소소한 개선

### Phase 2 (IndexedDB 저장) 구현 완료
- `db.js`: IndexedDB 래퍼 + 시드(seedIfEmpty) + recentSessions/lastResultFor/lastUsedWeight + clearAll.
- 정의(템플릿/운동/가이드)·설정을 DB에서 로드(최초 1회 SEED 시드). 세션은 `workoutSessions`에 저장.
- **지난번 기록** 자동 표시(운동별 최근 work 세트), **미정 중량**은 첫 기록값을 지난값으로 자동 채움.
- **기록 탭**: 날짜별 목록 + 상세(가이드/실제/평가/유산소). 통계는 여전히 이후(D9).
- 설정(휴식 시간) 변경 즉시 persist. 설정 → **전체 데이터 초기화**(2단계 확인) 동작.
- IndexedDB 불가 브라우저는 SEED 폴백으로 열람만 되게 처리.

### D15. 워밍업 = 입력 즉시 자동 기록 (옵션 A)
워밍업 세트는 별도 저장 버튼 없이 입력하는 즉시 세션에 기록되며, 각 행에 `✓ 기록됨` 표시. 삭제(✕)만 제공.

### D16. 날짜 어포던스 = 화살표만
상단 날짜의 "변경" 글자를 빼고 `›` 화살표 + 점선 밑줄로 탭 유도만.

**영향 문서**: `db.js`, `app.js`, `styles.css`, UX §15/§18, DATA §4~§7.

### D17. 입력 기본 무게 = 지난번 실제값 (절충안 C)
세트 입력칸 기본 무게 우선순위: 이번 세션 직전 세트 → **지난번 실제 무게** → 가이드값. 가이드·지난번 두 줄은 카드에 계속 표시해 "계획 대비 실제" 감각 유지. 가이드 변경 자체는 Phase 3/AI 코치에서.

---

## Phase 3 (부분) 완료 — 가이드 편집 + 요일 매핑 + 백업

- **가이드 편집 화면**: 템플릿별 종목 목록 → 편집(중량[미정 허용]·목표세트·최소/최대반복·필수/선택·워밍업 on/off·메모·순서 ▲▼·제거). 저장 시 `routineGuides`/`exercises`에 반영, 메모리 캐시 갱신. 기존 세션 기록은 불변.
- **지난값 넣기** 버튼: 편집 중 중량칸을 그 종목의 지난 실제값으로 채움(D4 승격의 실용 버전).
- **요일 매핑 편집**: 7요일 각각 템플릿 배정(없음 포함) → 즉시 저장.
- **JSON 백업/복원**(`backup.js` + 설정 연결): 내보내기(전체 스토어 → 파일), 복원(검증 → 스냅샷 → 덮어쓰기, 실패 시 기존 복구). 잘못된 파일 거부.
- **전체 데이터 초기화**: 2단계 확인 후 clearAll.

### 남은 것 (다음 세션)
- 템플릿 추가/삭제/이름변경 (요일 매핑은 이미 유연, 템플릿 CRUD만 남음).
- Phase 4: AI 코치(프롬프트 생성 / 제안 JSON 검증·diff·적용) — `ai-bridge.js`는 검증 함수만 구현된 스텁.
- 기록 통계(주간 세트·부위별) — D9로 이후.

---

## Phase 3 마무리(A) + 기록 통계(C)

### A. 템플릿 CRUD
가이드 편집 화면에서 템플릿 **추가/이름변경/삭제** + 템플릿에 **종목 추가**(새 커스텀 운동 생성) 가능.
- 삭제 시 해당 템플릿의 가이드도 제거하고, 요일 매핑에서 그 템플릿 참조를 null로 정리. 기존 세션 기록은 불변.
- 템플릿은 최소 1개 유지. 이로써 4분할 확장·새 루틴 생성이 앱 안에서 완결.

### C. 기록 통계 (최근 7일)
기록 탭 상단에 "최근 7일 요약" 카드: 운동 세션 수 · 총 본세트 · **부위별 세트(카테고리 집계)** · 유산소 횟수·시간 · **증량 후보 목록**. 날짜별 목록은 그 아래 유지. (D9의 "통계는 이후" 항목을 여기서 충족.)

### 남은 것
- Phase 4: AI 코치 (`ai-bridge.js` 검증 함수만 구현됨) — 다음 세션.

---

## Phase 4 완료 — AI 코치

`ai-bridge.js` 구현 + AI 탭 연결.
- **내보내기**: `프롬프트 생성` → 현재 요일배정·가이드(guideId 포함)·최근 4주 기록·증량 후보·제약(주3회·90분·유산소30분·데드리프트 제외·어시스트 역방향)을 텍스트로 조립 → 복사해서 ChatGPT/Claude에 붙여넣기.
- **가져오기**: AI가 준 `fitlog-guide-update` JSON 붙여넣기 → 검증(format/version/guideId 존재/숫자 유효성) → **변경안 diff를 항목별 체크박스로 표시** → 선택 적용.
- **적용**: update/remove/reorder 반영. 이전 가이드는 `guideHistory`에 `source:"ai-import"`로 저장. add는 안전상 이번 버전 제외(가이드 화면에서 직접 추가). 기존 세션 기록은 불변. 자동 적용 없음(항상 사용자 승인).

### 상태: MVP 전 Phase(1~4) 완료
- Phase 1 오늘/세트/타이머/워밍업/유산소/즉석 · Phase 2 IndexedDB 저장/지난번/기록 · Phase 3 가이드·템플릿 편집/요일매핑/백업 · Phase 4 AI 코치 · +기록 통계.
- 남은 개선 후보: AI add 액션 지원, 통계 확장, 아이콘/차임 최종본, 실제 iPhone PWA 설치 검증.

---

## 실사용 개선 1차 — 입력 UX 개편 (드래그 다이얼)

- **D18. 숫자 입력 = 드래그 다이얼**: +/− 스텝퍼·키패드 입력을 제거하고, 세로로 밀어 값을 조절하는 다이얼로 교체. 절충 가속(천천히=1스텝, 빠르게=여러 스텝). 짧게 탭하면 직접 입력. 단위: 중량 0.5kg / 반복 1회 / 시간 1분 / 경사 1 / 속도 0.1km/h. 큰 숫자 하나만 표시해 폰 화면 잘림 문제도 해소.
- **D19. 워밍업 입력 버그 수정**: 워밍업 세트 중량·반복이 다이얼로 값 반영되도록 수정(기존엔 입력 무반응).
- **D20. 완료 세트 수정**: 기록된 세트 우측 '수정' 버튼 → 다이얼 편집행 → 저장/취소.
- 유산소도 동일 다이얼 적용(숫자 잘림 해결).

### 다음(2차)
- 기록 상세에 실제 중량·세트 표시 + 저장된 기록 수정.
- 운동별 메모 + AI 코치 '특정일 교정' 모드.

---

## 실사용 개선 1단계 (C 자동갱신 + A 다이얼/헤더)

### C. 서비스워커 자동 갱신
- SW에 `message`(SKIP_WAITING) 핸들러 추가, index.html에 `updatefound`/`controllerchange` 로직.
- 새 버전 배포 시 앱을 열면 새 SW가 즉시 활성화되고 페이지가 자동 새로고침됨(최초 설치 땐 새로고침 안 함).
- → "아이콘 지웠다 다시 추가" 불필요. 같은 URL 유지되어 IndexedDB(기록)도 안전. CACHE_VERSION v4.

### A. 다이얼/헤더 개선
- **A-1/A-2 다이얼 분리**: 다이얼을 [왼쪽 숫자면(탭 → 직접 입력) | 오른쪽 그립(잡고 위아래 드래그)]로 분리. 손가락이 숫자를 가리지 않고, 탭 시 click 핸들러 안에서 동기 focus → iOS 키보드가 확실히 뜸.
- **A-3**: 워밍업/유산소/완료세트 수정 버튼 레이아웃 정렬.
- **A-4**: 알림 테스트 버튼을 헤더에서 오른쪽 정렬로 분리.

### 다음(2단계 = B)
- 기록 상세 중량·세트 표시 + 수정 / 운동별 메모 / AI 코치 특정일 교정 모드.

---

## 실사용 개선 2단계 (A-5/A-6 + B)
- **A-5**: 유산소 단위를 라벨로 이동(경사 / 속도 (km/h) / 시간 (분)), 칸엔 숫자만.
- **A-6**: 완료 세트 줄을 flex로 인라인 고정(수정 버튼이 아래로 내려가던 문제 해결).
- **B-1**: 기록 상세 '실제'에 중량 포함 표시(28kg · 12/11/9 또는 세트별 wkg×r). 저장된 기록을 운동별 '수정' → 다이얼로 세트/메모 편집 → 저장(status 재계산, DB 반영). 기록은 guideSnapshot 유지.
- **B-2**: 운동별 메모(카드 하단 입력, 기본 빈칸) → 세션 저장·기록 상세 표시.
- **B-3**: AI 코치 '특정일 교정' 모드 — 날짜 선택 → 그날 운동·실제세트·메모 담은 점검 프롬프트 생성(통증/자세/중량 조정 상담용).
- SW CACHE_VERSION v5.

---

## 실사용 개선 3단계 (SW 캐시전략 + 카드 초기화 + AI 프롬프트 개편)

- **SW network-first (assets)**: JS/HTML/manifest/icons는 cache-first(오프라인 보장), `assets/sounds`·`assets/video`는 network-first로 처리 → 사운드 등 에셋 교체 시 재배포하면 바로 반영(캐시 눌러붙는 문제 완화). CACHE_VERSION v7.
  - 주의(문서화): "Safari 전체 데이터 지우기"는 IndexedDB(기록)까지 삭제하므로 금지. 갱신은 앱 재실행 또는 아이콘 재추가로.
- **카드 초기화**: 운동 카드 하단 '본세트 초기화'(본세트만, 워밍업·메모 유지), 워밍업 영역 '워밍업 초기화'(워밍업만). 각각 confirm 1회.
- **AI 프롬프트 개편**: 전체 분석·특정일 교정 모두 "① 설명 자유롭게 → ② 가이드 적용 JSON은 맨 끝에 ```json 코드블록으로 분리" 방식으로 변경. 특정일 교정도 guideId를 포함해 **가이드 적용 가능**. 붙여넣기 검증은 설명+JSON 혼합 텍스트에서 fitlog-guide-update JSON을 자동 추출(코드펜스/중괄호 균형 파싱)하도록 개선.
  - 두 경로(특정일 교정 / 다일 종합분석) 모두 동일한 "설명받고 → JSON 붙여넣어 가이드 적용" 흐름.

- **기록 유산소 수정**: 기록 상세의 유산소 카드에도 '수정' 추가(경사·속도·시간 다이얼 편집→저장). SW v8.

---

## 화면 유지 강화 (C) — 세션 내내 + 3중 보장
- 문제: 타이머 도는 중에도 화면이 꺼져 알림이 먹통(무음 video가 iOS에서 수십 초 뒤 재생 풀림).
- 해결: **Screen Wake Lock API(주력, iOS 16.4+)** + 무음 video(폴백) + 8초 워치독(풀리면 재획득) + visibilitychange 복귀 시 재획득.
- 유지 범위를 "타이머 도는 동안"에서 **첫 사용자 탭(unlock)부터 운동 세션 내내**로 확대. `_finish`/`cancel`에서 화면 유지를 끄지 않음. 앱 백그라운드/종료 시 자연 해제.
- SW v9.

---

## 버그 수정: 다음 세트 미리 조정값이 타이머 종료 시 초기화되던 문제
- 증상: 세트 완료 후 타이머가 도는 동안 다음 세트 무게를 미리 조정해둬도, 타이머 종료(재렌더링) 시 그 값이 사라지고 '직전 세트값'으로 되돌아감.
- 원인: `renderActiveRow`가 재호출될 때마다 draft 객체를 새로 생성해 기본값으로 초기화.
- 수정: `res.draft`/`res.draftAt`에 진행 중 입력값을 세트 인덱스와 함께 보존, 같은 세트 재렌더링 시 그 값을 재사용. 세트 완료 시(`completeSet`)에만 draft를 정리해 다음 세트는 새 기본값으로 시작.
- SW v10.

---

## 실사용 개선 4단계 (2026-09-22) — 개선요청 5건 반영

기존 데이터/기능 보존 최우선. IndexedDB 스키마 버전 불변(추가 필드만, 마이그레이션 불필요). 기존 기록/가이드/설정/메모 미변경.

- **[항목1] AI 프롬프트 세트별 상세화**: gatherAIContext/buildPrompt 재작성. 기간 내 세션을 세션별·세트별 원본 그대로(개별 중량×반복수, 대표중량 통합 금지) 포함. 운동 메모, 가이드/실제 명확 구분, 목표세트 vs 실제세트 차이 표기. 저장 원본 기준(기록 미수정).
- **[항목2] 유산소 전용 종목(kind)**: exercises/guides에 `kind:"cardio"` 추가(없으면 strength로 간주=하위호환). 가이드 편집에서 `＋근력 종목`/`＋유산소 종목` 분리. 유산소는 시간/속도/경사만 입력(중량·반복·세트 미표시). 오늘 카드·기록 상세·통계·AI 프롬프트 모두 반영. 기존 세션 유산소(경사 걷기 고정 카드)와 공존.
- **[항목3] 기록 탭 분석 기간**: 시작/종료일 네이티브 date 선택, 기본 최근7일, 모든 요약지표+목록 재계산, 시작>종료 검증, localStorage(`fitlog-history-range`) 유지, `최근 7일` 초기화. 로컬 날짜 기준(UTC 드리프트 없음). 수행현황(완료/부족/미실시) 지표 추가.
- **[항목4] 타이머 종료 시 입력값 초기화 버그**: 이미 v10(res.draft/draftAt)에서 해결됨을 확인. 세트/운동 휴식 공통 적용. 재검증 완료.
- **[항목5] AI 코치 분석 기간**: AI 탭 독립 기간 UI. 첫 진입 시 기록탭 기간을 기본값으로, 이후 독립 변경(기록탭 불변). `기록 탭 기간 가져오기` 버튼. 프롬프트에 분석기간·세션수 명시. 빈 기간 시 "기록 없음" + 가이드/제약 유지.
- SW CACHE_VERSION v11. 변경 파일: app.js, ai-bridge.js, styles.css, service-worker.js.

---

## v23 (2026-09-23) — 탭바 뜸 / 운동 추가 검색창 가림 재수정

- **탭바 첫 실행 시 떠 있음**: 탭바 아래 빈 공간이 상단 safe-area(59pt)와 같다. iOS PWA가 첫 실행 때 뷰포트를 상태바만큼 짧게 잡고, 스크롤해야 복구되는 OS 버그라 CSS(env() 직접 사용)만으로는 해결 불가. → `index.html` 인라인 스크립트가 탭바 실제 바닥과 화면 바닥(standalone에선 `screen.height`)의 차이를 재서 `--vp-gap`으로 저장하고, `.tabbar`/`.modal-overlay`의 `bottom`을 그만큼 내린다. 정상이면 0이고, OS가 스스로 복구되면 재측정해서 0으로 돌아간다. 입력 중(키보드)에는 측정하지 않는다.
- **운동 추가 검색 시 검색창 가림**: iOS는 키보드가 떠도 레이아웃 뷰포트를 줄이지 않아 `inset:0` 오버레이의 시트가 키보드 뒤에 깔렸다. → 오버레이 top/height를 `visualViewport`(offsetTop/height)에 맞춘다. 키보드가 열리면 시트가 보이는 영역을 꽉 채워 헤더와 검색창은 위에 고정되고, 결과는 그 아래에서 스크롤된다.
- IndexedDB/데이터 변경 없음. 변경 파일: index.html, app.js, styles.css, service-worker.js.

## v24 (2026-09-23) — 탭바 문제 원인 재정의

- v23 실기 결과, 탭바는 제자리로 내려갔지만 **아래 59pt가 잘렸다**. 즉 첫 실행 때 앱 화면(WebView) 자체가 상태바 높이만큼 짧게 잡혀서 그 아래는 그릴 수 없다. `--vp-gap` 보정은 제거했다.
- 오늘 탭만 증상이 보이는 이유: 유산소처럼 내용이 짧으면 페이지가 스크롤되지 않는다. iOS는 스크롤이 한 번 일어나야 화면을 전체 높이로 다시 계산한다. 다른 탭은 내용이 길어 스크롤하는 순간 복구된다.
- 수정: `index.html` 인라인 스크립트가 standalone 세로 모드에서 `screen.height - innerHeight`가 20~100이면 짧게 잡힌 것으로 판단한다. 그러면 투명 여백을 잠깐 붙여 1px 스크롤했다 되돌린다(실행 시·복귀 시 최대 6회).
- 진단: 설정 → 앱 버전 옆에 `화면 첫값→현재값/화면높이 · 보정 횟수` 표시.
- 운동 추가 시트: 키보드가 열렸을 때 시트 높이에서 safe-area-top + 10px을 빼서 상단이 상태바·다이나믹 아일랜드에 가리지 않게 했다.

---

## v25 (2026-09-23) — 3D 운동 애니메이션 미리보기 (Astra 패키지 통합)

- **자산**: Astra가 v18 기준으로 만든 패키지를 호환성 분석한 결과, 자산은 재작업 없이 호환됐다. 운동 ID, 근육 노드 20개, 관절 17개, Three r180 바이트가 모두 일치하고 히트맵 `human_3d.glb`도 변경되지 않았다. 원본 그대로 추가한 파일은 `assets/exercises/{barbell,dumbbell,cable}_curl.glb`, `data/exercise-preview-map.json`, `data/animation-muscle-map.json`, `runtime/exercise-preview-3d.js`다. 히트맵 GLB와 애니메이션 GLB는 별개이며 서로 교체하지 않는다.
- **등록**: `catalog-data.js` 끝에 `EXERCISE_PREVIEWS`(세 ID)를 추가만 했다. 기존 CATALOG·MUSCLE_MAP·EXISTING_LINK는 그대로다.
- **D21. 3D는 상세 화면에서만**: 목록 행에 3D를 바로 띄우지 않는다. WebGL 개수 제한과 발열 때문이다. 목록 선택과 찾아보기 둘 다 행의 이름을 탭하면 상세로 가고, `＋`는 지금처럼 바로 추가한다. 3D가 있는 운동은 행에 `3D` 배지를 붙인다.
- **D22. 상세의 뒤로·추가 완료는 들어온 화면으로**: 목록의 검색어·필터·스크롤과 찾아보기의 검색어·스크롤을 복원한다. 검색어가 남아 있으면 키보드를 다시 띄우지 않는다. 첫 화면에서 새로 들어가면 초기 상태로 시작한다.
- **수명주기**: 시트마다 컨트롤러는 1개이고 히트맵의 `_heatCtrls`와 따로 관리한다. `ctx.go`와 `close`에서 화면을 지우기 전에 동기적으로 destroy하고, 세대 번호로 늦게 도착한 import·GLB 결과는 버린다. 로딩에 실패하면 3D 슬롯만 숨기고 설명과 추가 버튼은 유지한다.
- **UI**: 3D 영역 320px, 재생/일시정지 버튼, 주요(#ff825f)·보조(#e5bf70) 부위 범례. 운동 부위를 안내하는 것이지 근육 활성도를 측정하는 것은 아니다.
- **D23. GLB 캐시는 앱 버전과 함께 교체**: SW에서 `/assets/exercises/`는 캐시에 있으면 캐시를 쓰고, 없으면 받은 뒤 성공한 응답만 저장한다. 설치 때 미리 받지 않고 본 운동만 저장한다. 버전을 올리면 삭제되고 다음에 볼 때 다시 받는다.
- **배포 제외**: `_astra_review/`는 `.gitignore`에 넣었다. 선택형 어댑터 `exercise-preview-mount.js`는 같은 로직을 app.js에 넣었기 때문에 쓰지 않는다.
- **검증**: 로컬 `/fitlog/` 하위 경로 + Chromium(SwiftShader) 393×852 터치 환경에서 21개 항목이 PASS했다. iPhone 15 실기기(성능·발열·제스처)는 NOT_TESTED다.

---

## v26 (2026-09-23) — Astra 3D 확장 1단계 대표 4종 시험 통합 (technical_prototype)

- **D24. 시제품은 정식 미리보기와 분리**
  - 대상: `barbell_bench_press`, `push_up`, `lat_pulldown`, `treadmill_incline_walk`. 상태는 `technical_prototype`이며 품질 승인 전이다.
  - `EXERCISE_PREVIEWS`와 운동 상세 화면에는 등록하지 않았다. 목록에 3D 배지도 없고 상세에 슬롯도 없다.
  - 확인 경로: **설정 → 개발자 · 시험 기능 → 3D 시제품 시험 → 열기**
  - 시험 화면에는 '기술 시제품' 배지와 '품질 검증 전' 안내를 표시한다.
- **D25. 두 가지 로딩 구조를 함께 쓴다**
  - 컬 3종은 기존 `runtime/exercise-preview-3d.js`(운동별 완결 GLB)를 그대로 쓴다.
  - 시제품은 신규 `runtime/exercise-prototype-3d.js`를 쓴다. 공유 인체 1회 로드 + 클립 GLB + 별도 장비 GLB 구조다.
  - 신규 모듈은 Astra의 `prototype-viewer.js`를 참고해 앱 구조에 맞게 다시 작성했다(통째 교체 아님). 추가한 검사·기능:
    - 공유 인체 sha256·관절 검사, 클립 트랙 바인딩 검사, 경로 안전성 검사
    - 운동별 manifest 카메라 적용(좁은 화면에선 높이를 늘려 가로 폭 확보)
    - 장비 모드 fixed/twoHand/장비 없음, 열린 손
    - 케이블 양 끝을 manifest의 장비 노드 기준으로 계산(컬용 고정 도르래 좌표 미사용)
    - 세대 번호로 늦은 결과 폐기, destroy 시 전체 해제
  - 강조는 앱 카탈로그 매핑의 primary/secondary만 쓴다. minor는 중립색이다.
- **자산 위치**
  - 자산: `assets/prototype/stage1/{shared,clips,equipment}/`
  - manifest: `data/prototype/stage1-manifest.json` (Astra 원본 그대로)
  - 넣지 않은 것: 비교용 A′ GLB, contacts JSON, `reference/`, `legacy/`, 이미지, 제작 스크립트
- **SW**: `/assets/prototype/`는 시험 화면에서 요청한 것만 성공 시 저장한다. 미리 받지 않고, 앱 버전을 올리면 함께 교체된다.
- **배포 제외**: `.gitignore`에 `_astra_stage1/`을 추가했다.
- **남은 품질 문제(해결되지 않음, Astra 보완 예정)**
  - 벤치: 어깨·팔 표면 비틀림
  - 푸시업: 손목·어깨 각짐
  - 랫풀다운: 오버헤드 어깨·골반 변형, 기둥이 몸을 가림
  - 경사 걷기: 발 구름 없음, 무릎·골반 접힘, 반복 연결부 속도 불연속
- **검증**
  - Chromium(SwiftShader) 393×852·320×568에서 47개 + 기존 회귀 21개 PASS
  - iPhone 실기기는 NOT_TESTED

## v27 (2026-09-24~25) ① 시제품 시험 화면 레이아웃 수정 + 실기기 품질 피드백 분류

- **수정(앱)**: 시험 시트가 낮은 화면에서 줄어들면서 안내 박스가 얇은 띠로 찌그러지고 상태 문구가 잘리던 문제. 원인은 `.proto-body`의 자식 요소가 줄어들도록 잡힌 CSS였다. `flex-shrink:0`으로 바꾸고 시트 안에서 스크롤되게 했다.
- **iPhone 피드백(사용자)**: 경사 걷기의 팔꿈치 역관절과 허벅지 찢어짐, 랫풀다운을 머리 뒤로 당기는 동작.
- **수치 확인 결과, 모두 Astra 자산 문제로 분류**:
  - 경사 걷기 팔꿈치: 전완이 상완보다 뒤로 꺾임(방향값 −0.84~−1.15, 굽힘 55~70°) → 클립 수정 필요
  - 허벅지 찢어짐: `muscle_adductors` 삼각형이 원래 대비 최대 98배(걷기)·164배(랫풀다운)·41배(벤치)까지 늘어남 → 공유 리그 다리 스키닝 수정 필요(rig-v2)
  - 랫풀다운: 손목이 머리 중심보다 0.043m 뒤쪽 수직선으로 이동(비하인드넥)하고, 도르래도 z −0.04 → 클립 경로와 기구 배치를 가슴 앞쪽 당기기로 변경 필요
- 앱에서 클립을 억지로 보정하지 않는다. 동작 원본과 앱이 어긋나고, 스키닝 문제는 앱에서 해결할 수 없기 때문이다.

## v27 ② Astra Repair R2 대표 4종 적용 (여전히 technical_prototype)

- **자산 묶음 교체** (`apply-files.json`의 크기·SHA-256과 전부 일치)
  - 공유 인체 `assets/prototype/stage1/shared/human-rig-v2.glb` 추가: rig-v2.0.0, 19관절(기존 17개 + toe_L/R)
  - 클립 4개 교체(같은 파일명, rig-v2 전용, CUBICSPLINE): `assets/prototype/stage1/clips/`
  - 장비 `equipment/bench.glb`, `lat_station.glb` 교체. barbell·lat_bar·open_hands·treadmill은 변경이 없어 유지
  - `data/prototype/stage1-manifest.json` 교체: 시제품 4종 전용 파일이라 컬 3종 등록(`exercise-preview-map.json`)에는 영향 없음
  - `human-rig17-v1.glb`는 롤백 대비로 남겼다. manifest에서는 참조하지 않는다.
- **리그 계약 파일 추가**: `data/prototype/rig-contract-rig-v2.0.0.json`. rigId별 파일명이라 다른 리그와 섞이지 않는다.
- **런타임** (`runtime/exercise-prototype-3d.js`, 호환성 검사만 추가하고 동작 보정은 없음)
  - GLB 루트 extras의 rigId, 클립의 rigSha256 검사
  - 계약 기준 19개 관절의 이름·중복·부모 계층·바인드 위치·스켈레톤 순서 검사
  - 20개 근육 노드와 독립 재질 검사
  - 클립 이름 = catalogId, 19개 회전 + pelvis 위치 트랙이 모두 있어야 함
  - `crypto.subtle`이 없으면 무결성 검사 불가로 로딩 거부
  - 진단 정보에 rigId를 표시하고, 시험 화면 상태 문구에도 `리그 rig-v2.0.0`을 보여준다.
  - 17관절 하드코딩은 원래 없었다. 자산만 교체해도 재생되는 것을 먼저 확인한 뒤 검사를 추가했다.
- **SW 수정 (캐시 혼용 실측 후)**
  - 증상: v26 → v27 업데이트 시뮬레이션에서 새 SW 설치가 브라우저 HTTP 캐시에 남은 구버전 파일(`app.js` v26, rig17 manifest)을 새 캐시에 담았다. GitHub Pages는 `max-age=600`으로 응답한다.
  - 수정: 설치 시 `cache:"reload"`로 서버에서 새로 받는다. 3D·데이터 자산은 캐시 미스일 때 `cache:"no-cache"`로 서버 재검증 후 저장한다.
  - 재시험 결과: v26 캐시가 삭제되고, rig17 요청 없이 rig-v2 묶음만 캐시된다.
- **수정 전후 수치** (앱과 같은 Three r180)
  - 걷기 팔꿈치 굽힘 방향: −1.15~−0.84 → +0.30~+0.40
  - 걷기 최대 메시 늘어남: 108.9배 → 1.61배. 반복 경계 속도 튐: 0.813 → 0.046 m/s. 발가락 회전 22.6°
  - 랫풀다운 손목: 머리 뒤 4.3cm → 머리 앞 30~31.5cm, 최저 1.075m(윗가슴 높이). 최대 늘어남 164배 → 5.18배(몸통, 겨드랑이 쪽 추정)
  - 벤치 손 반폭 0.285m. 푸시업 손은 바닥 0, 몸 최저점 −8.5mm
- **남은 문제 (Astra 보고, 앱에서 가리지 않음)**: 오버헤드 겨드랑이 국부 신장, 전완·손목 압축·비틀림, 걷기 손 모양, 푸시업 발끝 바닥 침투 약 8mm
- **검증**
  - Chromium(SwiftShader): 전체 52 + R2 호환·혼용 거부 13 + 기존 추가 흐름 회귀 21 + 업그레이드 시뮬레이션 PASS
  - iPhone 실기기: NOT_TESTED
- **배포 제외**: `.gitignore`에 `_astra_repair_r2/`를 추가했다.
